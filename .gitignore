supabase/.temp/

# setup.ts wizard progress (holds the project ref + step state; no secrets)
.capybara-setup-state.json

# Environment / secrets — never commit these
.env
.env.*
!.env.example

# Pronunciation-deck generator: synthesized audio, built decks, and any phrase
# list exported from the corpus. Generated audio is Vika's voice and the phrase
# lists are conversation-derived — this repo is public, so none of it is committable.
__pycache__/
scripts/anki_pronunciation/.cache/
scripts/anki_pronunciation/dist/
scripts/anki_pronunciation/.autopull-state.json
scripts/anki_pronunciation/.autopull.log
*.apkg
phrases-*.json
